trigger:
- main

stages:
- stage: Release
  jobs:
  - job: J2
    pool:
      vmImage: ubuntu-22.04
    steps:
      - script: |
          sudo apt install software-properties-common
          sudo add-apt-repository ppa:deadsnakes/ppa
          sudo apt install python3.9

          sudo apt install python3.9-distutils
          curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
          python3.9 get-pip.py

          python3.9 -m pip install -U iotedgedev pyOpenSSL==22.0.0 urllib3==1.22 requests
        displayName: 'Command Line Script'
      - task: AzureIoTEdge@2
        inputs:
          action: 'Build module images'
          templateFilePath: 'deployment.template.json'
          defaultPlatform: 'arm64v8'
        displayName: 'Build img'
      
      - task: AzureIoTEdge@2
        inputs:
          action: 'Push module images'
          containerregistrytype: 'Azure Container Registry'
          azureSubscriptionEndpoint: 'Azure for Students(4)(cdbe522d-a95d-4d8c-b999-af7984931bc9)'
          azureContainerRegistry: '{"loginServer":"containerregistrytfm.azurecr.io"}'
          templateFilePath: 'deployment.template.json'
          defaultPlatform: 'arm64v8'
          fillRegistryCredential: 'true'
        displayName: 'Push img'
      - task: AzureIoTEdge@2
        inputs:
          action: 'Generate deployment manifest'
          templateFilePath: 'deployment.template.json'
          defaultPlatform: 'arm64v8'
          deploymentManifestOutputPath: '$(System.DefaultWorkingDirectory)/config/deployment.json'
          validateGeneratedDeploymentManifest: 'false'
        displayName: 'Generate deployment manifest'
      
      - task: AzureIoTEdge@2
        inputs:
          action: 'Deploy to IoT Edge devices'
          deploymentFilePath: '$(System.DefaultWorkingDirectory)/config/deployment.json'
          azureSubscription: 'Azure for Students(4)(cdbe522d-a95d-4d8c-b999-af7984931bc9)'
          iothubname: 'IoTHubPlantTFM'
          deploymentid: '$(System.TeamProject)-devops-deployment'
          priority: '0'
          deviceOption: 'Single Device'
          deviceId: 'raspberrypi4'
        displayName: 'Deploy to IoT Edge devices'